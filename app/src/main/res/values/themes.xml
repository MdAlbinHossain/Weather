<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.Weather" parent="android:Theme.Material.Light.NoActionBar" />

    <style name="Theme.Weather.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@drawable/cloudy_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/cloudy_foreground</item>
        <item name="postSplashScreenTheme">@style/Theme.Weather</item>
    </style>
</resources>